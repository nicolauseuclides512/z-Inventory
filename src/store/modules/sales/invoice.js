import axios from 'axios'
import { responseOk } from 'src/helpers'

const state = {
  invoice: {}
}

const mutations = {}

const actions = {}

export default {
  namespaced: true,
  state,
  mutations,
  actions,
}
